# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Claude Code + scratch dirs (session-local state; never belongs in repo)
.claude/
.context/

# Secrets (don't accidentally commit local creds)
.env
.env.local
api.txt

# Node
node_modules/
